# This workflow has been disabled and removed
# Original functionality has been integrated into main.yml

name: Economic Indicator (DISABLED)

on:
  # Disable all triggers
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Workflow Disabled
      run: |
        echo "❌ This workflow has been disabled and removed"
        echo "🔄 Functionality has been integrated into main workflow"
        echo "📍 This file exists only to override GitHub cache"
        exit 1