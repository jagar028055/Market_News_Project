# This workflow has been disabled and removed
# Deployment functionality is handled by main.yml

name: Staging Deployment (DISABLED)

on:
  # Disable all triggers
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Workflow Disabled
      run: |
        echo "❌ This workflow has been disabled and removed"
        echo "🔄 Deployment handled by main.yml workflow"
        echo "📍 This file exists only to override GitHub cache"
        exit 1